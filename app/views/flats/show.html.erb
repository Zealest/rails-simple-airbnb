<h1><%= @flat.name %></h1>

<p><%= @flat.address %></p>
<p><%= @flat.description %></p>
<p>Price per night: <%= @flat.price_per_night %> €</p>
<p>Max guests: <%= @flat.number_of_guests %></p>

<%= link_to 'Edit Flat', edit_flat_path(@flat), class: 'btn btn-warning' %>
<%= link_to 'Back to Flats', flats_path, class: 'btn btn-secondary' %>
<%= link_to 'Delete Flat', flat_path(@flat), method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-danger' %>
